# gestion-etudiants
Application de gestion des étudiants + Carte éetudiant
 for  run database  u need docker docker and this command :
cd docker
docker-compose up -d
pour cree une image dans docker posgress.yaml 
docker compose -f posgress.yaml up -d